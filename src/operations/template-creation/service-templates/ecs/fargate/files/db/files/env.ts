export const envExample = `# Copy the following ( change applicable values for your setup ) to \`.env.local\`
export POSTGRES_PASSWORD=password1
export POSTGRES_USER=postgres
export POSTGRES_DB=musiclab`

export const envLocal = `export POSTGRES_PASSWORD=password1
export POSTGRES_USER=postgres
export POSTGRES_DB=musiclab`
